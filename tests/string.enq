import std::string::String


fn main() -> u32 {
    let a = String::from_str("hello")
    let b = String::new()

    if a.as_str() != "hello" {
        ret 1_u32
    }
    if a.len() != 5_usize {
        ret 1_u32
    }
    if !b.is_empty() {
        ret 1_u32
    }

    ret 0_u32
}
